<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vk.ai.mapper.ModelListMapper">

    <select id="upTokenLimit">
        UPDATE model_list
        SET token_limit = CASE  WHEN token_limit >= #{usedTokens} THEN token_limit - #{usedTokens}  ELSE 0
            END
        WHERE id = #{modelId}
    </select>
</mapper>
